<div style="margin: 20px 0;">
  <p>
    Menu:
    <%= link_to 'Daftar Buku', root_path %> |
    <%= link_to 'Daftar Penulis', authors_path %> |
    <% if current_user %>
      Hi! <%= "#{current_user.name}" %>
      <%= link_to 'Logout', session_path(current_user), method: :delete %>
    <% else %>
      <%= link_to 'Login', signin_path %>
    <% end %>
  </p>
</div>
